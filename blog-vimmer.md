# お前もVimmerにならないか 👊

「Vimって難しそう…」「VSCodeで十分じゃない？」

そう思っていた時期が私にもありました。

でも一度Vim（正確にはNeovim）の世界に足を踏み入れると、もう戻れません。
今日はその沼への入り口をご案内します。

## なぜ今さらVimなのか

1. **キーボードから手を離さない快感** - マウスに手を伸ばすたび、0.5秒の人生を無駄にしていませんか？
2. **軽い。とにかく軽い** - Electron製エディタとは比べ物にならない起動速度
3. **どこでも同じ環境** - `~/.config/nvim` をGitで管理すれば、どのマシンでも同じ開発体験
4. **かっこいい** - ターミナルでカタカタやってるの、正直かっこいいですよね

## 私のNeovim環境

モダンな開発に必要なものは全部揃えました：

| 機能 | プラグイン | 説明 |
|------|-----------|------|
| 🎨 カラースキーム | catppuccin | 目に優しいモダンなテーマ |
| 📁 ファイル操作 | oil.nvim | バッファのようにディレクトリを編集 |
| 🔍 ファジーファインダー | telescope.nvim | 爆速ファイル検索・grep |
| 🧠 LSP | mason + lspconfig | VSCode並みの補完・定義ジャンプ |
| 🌳 シンタックス | treesitter | モダンな構文ハイライト |
| 🐙 Git | lazygit + gitsigns | Git操作がエディタ内で完結 |
| 📝 フォーマット | conform.nvim | Prettier等で自動整形 |
| 💻 ターミナル | toggleterm | エディタ内でターミナル起動 |

## 最初に覚える5つだけ

Vimmerへの第一歩。まずこれだけ覚えてください：

```
jj        → Escの代わり（挿入モードから抜ける）
<Space>w  → 保存
<Space>e  → ファイルエクスプローラー
<Space><Space> → ファイル検索
<Space>fg → grep検索
```

「え、たった5つ？」

そう、たった5つです。あとは `<Space>` を押して待てば、`which-key` が次に押せるキーを教えてくれます。

## VSCodeと両立できる

「いきなりNeovim一本は怖い…」

安心してください。この設定は **VSCode Neovim拡張** にも対応しています。

```lua
if vim.g.vscode then
  require("config.vscode")
else
  -- 通常のNeovim設定
end
```

VSCode内でVimキーバインドを使いながら、徐々に本物のNeovimに移行できます。

## 見た目もモダン

「ターミナルは地味でしょ？」

2024年のNeovimを舐めないでください。

- **noice.nvim** でコマンドラインやメッセージがおしゃれに
- **bufferline.nvim** でタブ風のバッファ表示
- **lualine.nvim** で情報豊富なステータスライン

True Color対応のターミナル（iTerm2等）なら、VSCodeに負けない見た目になります。

## セットアップは3ステップ

```bash
# 1. 必要なツールをインストール
brew install neovim ripgrep
brew install --cask font-hack-nerd-font

# 2. 設定をクローン
git clone <your-config-repo> ~/.config/nvim

# 3. 起動（プラグインは自動インストール）
nvim
```

lazy.nvimのおかげで、初回起動時にプラグインが自動でインストールされます。

## AIエージェントはどうするの？

「でも今どきAIなしで開発するの？」

ご安心ください。むしろNeovimの方が快適かもしれません。

私は **toggleterm** でフローティングターミナルを開いて、そこでAIエージェントのCLIを叩いています。

```
<Space>t → ターミナルを開く
$ claude      # Claude Code
$ aider       # Aider
$ cursor-cli  # etc...
```

この方法のメリット：

- **エディタを選ばない** - AIエージェントはCLIなので、どのエディタでも同じ体験
- **複数のAIを使い分け** - `1<Space>t` でClaude、`2<Space>t` でAider、みたいな運用も可能
- **Vimキーバインドが使える** - ターミナル内でもVimの操作感はそのまま

GUIに依存しないからこそ、AIツールの進化にも柔軟に対応できます。

## よくある質問

**Q: 設定が複雑そう…**
A: 最初は誰かの設定をそのまま使えばOK。慣れてきたら少しずつカスタマイズしていきましょう。

**Q: 矢印キー使っちゃダメ？**
A: 最初は使ってもいいです。そのうち `hjkl` の方が速いと気づきます。

**Q: プラグインが動かない！**
A: `:checkhealth` を実行。大体の問題はこれで原因がわかります。

## 最後に

Vimは確かに学習曲線があります。でも、その曲線を超えた先には、キーボードと一体化したような、流れるようなコーディング体験が待っています。

さあ、お前もVimmerにならないか？ 👊

---

この設定ファイルは [GitHub](リンク) で公開しています。
質問があればお気軽にどうぞ！
